modifier = {
	pop_has_trait = trait_nerve_stapled
	factor = 1.1
}
modifier = {
	OR = {
		pop_has_trait = trait_robot_domestic_protocols
		pop_has_trait = trait_charismatic
	}
	factor = 1.5
}

{{#each cow_bonus as |mult trait|}}
modifier = {
	pop_has_trait = trait_lv_{{ trait }}
	factor = {{ getWeight mult }}
}
{{/each}}
