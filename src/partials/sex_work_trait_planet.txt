{{#each sex_bonus}}
triggered_planet_modifier = {
	potential = { has_trait = trait_lv_{{@key}} }
	modifier = {
		planet_amenities_add = {{ multiply ../amenities this }}
		trade_value_add = {{ multiply ../trade this }}
	}
}
{{/each}}
